/**
 * Domain Index
 *
 * Central entry point for all domain modules.
 * Import from here for discoverable access to domain logic.
 *
 * @example
 * ```typescript
 * import { useToolHealth } from '@/domain'
 * // or
 * import { useToolHealth } from '@/domain/tool-health'
 * ```
 */

export * from './tool-health'
