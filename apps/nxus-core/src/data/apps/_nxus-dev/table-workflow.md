# Table Architecture Workflow

Set `ARCHITECTURE_TYPE='table'` in `config/feature-flags.ts`

## Fresh Setup

Populate items table:
{{command:seed-legacy}}

## After UI Changes

Export to manifest files:
{{command:export}}
