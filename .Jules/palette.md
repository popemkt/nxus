## 2024-05-22 - Accessible HUD Controls
**Learning:** Central navigation components often rely on iconography which is inaccessible without explicit labels. Adding `aria-label` and `aria-pressed` transforms a confusing "icon soup" into a navigable tool for screen reader users.
**Action:** Check all `Floating*` components for missing ARIA attributes.
